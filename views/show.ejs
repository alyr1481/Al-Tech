<% include partials/header %>

<div class="row">
  <div class="col s12">
    <div class="card">
      <div class="card-image">
        <img src="<%= post.image %>">
        <span class="card-title title-span"><%= post.title %></span>
      </div>
      <div class="card-content">
        <p><%- post.content %></p>
      </div>
    </div>
  </div>
</div>

<% include partials/footer %>
